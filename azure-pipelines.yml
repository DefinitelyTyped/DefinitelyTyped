trigger:
  - master

pr: none

jobs:
  - checkout: self
    fetchDepth: 1
    fetchTags: false

  - job: npmRunTest
    pool:
      vmImage: ubuntu-latest
      demands: npm

    timeoutInMinutes: 360

    steps:
      - script: |
          echo "Disabled"
        displayName: 'Do nothing'

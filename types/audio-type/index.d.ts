export default function audioType(buf: Uint8Array | ArrayBuffer | null): string | undefined;

export function isMp3(buf: Uint8Array | ArrayBuffer | null): boolean;

export function isWav(buf: Uint8Array | ArrayBuffer | null): boolean;

export function isOgg(buf: Uint8Array | ArrayBuffer | null): boolean;

export function isFlac(buf: Uint8Array | ArrayBuffer | null): boolean;

export function isM4a(buf: Uint8Array | ArrayBuffer | null): boolean;

export function isOpus(buf: Uint8Array | ArrayBuffer | null): boolean;

export function isQoa(buf: Uint8Array | ArrayBuffer | null): boolean;

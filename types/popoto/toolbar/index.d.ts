export namespace Toolbar {
    let TOOL_TAXONOMY: string;
    let TOOL_RELATION: string;
    let TOOL_CENTER: string;
    let TOOL_FULL_SCREEN: string;
    let TOOL_RESET: string;
    let TOOL_SAVE: string;
    let TOOL_FIT_TEXT: string;

    function render(container: SVGGElement): void;
}

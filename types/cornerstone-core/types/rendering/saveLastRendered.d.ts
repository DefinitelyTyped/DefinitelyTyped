/**
 * Saves the parameters of the last render into renderingTools, used later to decide if data can be reused.
 *
 * @param enabledElement Cornerstone EnabledElement
 * @returns enabledElement.renderingTools
 */
export default function _default(enabledElement: any): any;

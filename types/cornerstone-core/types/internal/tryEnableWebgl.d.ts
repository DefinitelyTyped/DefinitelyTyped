/**
 * Checks if webGL is supported and initializes the rendering engine.
 * @param options Options to check if webgl rendering is requested (e.g. enable webgl by passing {renderer: 'webgl'})
 * @returns true if webgl rendering has been successfully initialized. Otherwise, false.
 */
export default function _default(options: any): boolean;

import * as React from "react";
// import * as ReactDOM from "react-dom";
import JSONTree from "react-json-tree";

<JSONTree data={{
    string: "string",
    array: [1, 2, 3],
    bool: true,
    object: {
        foo: "bar"
    }
}} />;

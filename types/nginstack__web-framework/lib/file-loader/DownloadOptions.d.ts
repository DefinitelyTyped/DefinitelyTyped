export = DownloadOptions;
declare function DownloadOptions(opt_options?: { fileName?: string }): void;
declare class DownloadOptions {
    constructor(opt_options?: { fileName?: string });
    fileName: string;
}

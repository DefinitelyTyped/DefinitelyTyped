/**
 * Map `visit` over an array of objects.
 *
 * @param collection - The object containing the method to call
 * @param method - The name of the method to call
 * @param arr - Array of objects to visit
 */
declare function mapVisit(collection: object, method: string, arr: any[]): void;

export = mapVisit;

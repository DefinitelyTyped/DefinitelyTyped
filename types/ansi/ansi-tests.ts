import ansi = require('ansi');

/*
const cursor = ansi(process.stdout);

Object.keys(ansi.colors).forEach((color) => {
    cursor[color]().bold().write('Hello, bold ' + color + ' world!').reset();
});
*/
import * as constants from 'time-constants';

let test: number;

test = constants.MILLISECONDS_PER_SECOND;
test = constants.SECONDS_PER_MINUTE;
test = constants.MINUTES_PER_HOUR;
test = constants.HOURS_PER_DAY;
test = constants.DAYS_PER_WEEK;
test = constants.MONTHS_PER_YEAR;

test = constants.SECOND;
test = constants.MINUTE;
test = constants.HOUR;
test = constants.DAY;
test = constants.WEEK;
test = constants.YEAR;
test = constants.NORMAL_YEAR;
test = constants.LEAP_YEAR;
test = constants.DECADE;
test = constants.HALF_YEAR;
test = constants.AVERAGE_MONTH;

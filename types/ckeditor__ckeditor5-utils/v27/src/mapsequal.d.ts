/**
 * Checks whether given {Map}s are equal, that is has same size and same key-value pairs.
 *
 */
export default function mapsEqual(mapsA: Map<unknown, unknown>, mapsB: Map<unknown, unknown>): boolean;

/**
 * For a given element, returns the nearest ancestor element which CSS position is not "static".
 */
export default function getPositionedAncestor(element?: null): null;
export default function getPositionedAncestor(element: HTMLElement): Element | null;

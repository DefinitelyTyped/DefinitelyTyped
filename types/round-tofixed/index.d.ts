/**
 * round number `x` to `digits` number of digits after decimal point
 * @param val
 * @param digits positive integer, defaults to 0
 */
declare function roundToFixed(val: number, digits?: number): number;

export = roundToFixed;

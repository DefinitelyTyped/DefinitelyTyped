/**
 * Base props object for {@link core.Node}.
 *
 * Key determines whether props changed or the whole node changed upon
 * re-render.
 *
 * @typedef {{key?: string}} Props
 */
export declare interface Props
{
    key?: string;
}

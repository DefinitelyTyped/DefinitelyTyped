import Node from "../core/Node.js";

export const linearToneMapping: (color: Node, exposure: Node) => Node;

export const reinhardToneMapping: (color: Node, exposure: Node) => Node;

export const cineonToneMapping: (color: Node, exposure: Node) => Node;

export const acesFilmicToneMapping: (color: Node, exposure: Node) => Node;

export const agxToneMapping: (color: Node, exposure: Node) => Node;

export const neutralToneMapping: (color: Node, exposure: Node) => Node;

import { ComponentType } from 'react';

declare namespace ScrollLock {
    interface Props {
        children?: never;
    }
}
declare const ScrollLock: ComponentType<ScrollLock.Props>;

export default ScrollLock;

// ------------------------------- NODE MODULES -------------------------------

import clear from 'clear';

// ------------------------------ CUSTOM MODULES ------------------------------

// -------------------------------- VARIABLES ---------------------------------

// ----------------------------- FILE DEFINITION ------------------------------

clear();

clear({});

clear({
    fullClear: true,
});

clear({
    fullClear: false,
});

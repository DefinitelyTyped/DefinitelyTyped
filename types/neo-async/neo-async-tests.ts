import * as asyncLib from "neo-async";

asyncLib.forEach([], () => {});
asyncLib.forEach([], () => {}, () => {});

asyncLib.map([], () => {});
asyncLib.map([], () => {}, () => {});

asyncLib.parallel([], () => {});

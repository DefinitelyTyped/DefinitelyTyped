import { White } from './adapt';

export const CATs: Record<string, any>;

export function defineCAT(options: { id: string; toCone_M: number[][]; fromCone_M: number[][] }): void;

export function adapt(W1: White, W2: White, id?: string): number[];

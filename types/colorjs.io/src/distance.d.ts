import Color, { ColorObject } from './color';

/** @todo make sure that param types are right */
export default function distance(color1: Color | ColorObject, color2: Color | ColorObject, space: string): number;

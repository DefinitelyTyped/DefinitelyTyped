// AUTO-GENERATED: do not modify this file directly.
// If you need to make changes, modify generate-fp.ts (if necessary), then open a terminal in types/lodash/scripts, and do:
// npm run fp

interface InvokeMap {
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (): InvokeMap;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (methodName: string): InvokeMap1x1;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (p1: _.__, args: ReadonlyArray<any>): InvokeMap1x2;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (methodName: string, args: ReadonlyArray<any>): InvokeMap1x3;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (p1: _.__, p2: _.__, collection: object | null | undefined): InvokeMap1x4;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (methodName: string, p2: _.__, collection: object | null | undefined): InvokeMap1x5;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (p1: _.__, args: ReadonlyArray<any>, collection: object | null | undefined): InvokeMap1x6;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (methodName: string, args: ReadonlyArray<any>, collection: object | null | undefined): any[];
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    <TResult>(method: (...args: any[]) => TResult): InvokeMap2x1<TResult>;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    <TResult>(method: (...args: any[]) => TResult, args: ReadonlyArray<any>): InvokeMap2x3<TResult>;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    <TResult>(method: (...args: any[]) => TResult, p2: _.__, collection: object | null | undefined): InvokeMap2x5<TResult>;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    <TResult>(method: (...args: any[]) => TResult, args: ReadonlyArray<any>, collection: object | null | undefined): TResult[];
}
interface InvokeMap1x1 {
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (): InvokeMap1x1;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (args: ReadonlyArray<any>): InvokeMap1x3;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (p1: _.__, collection: object | null | undefined): InvokeMap1x5;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (args: ReadonlyArray<any>, collection: object | null | undefined): any[];
}
interface InvokeMap1x2 {
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (): InvokeMap1x2;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (methodName: string): InvokeMap1x3;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (p1: _.__, collection: object | null | undefined): InvokeMap1x6;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (methodName: string, collection: object | null | undefined): any[];
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    <TResult>(method: (...args: any[]) => TResult): InvokeMap2x3<TResult>;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    <TResult>(method: (...args: any[]) => TResult, collection: object | null | undefined): TResult[];
}
interface InvokeMap1x3 {
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (): InvokeMap1x3;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (collection: object | null | undefined): any[];
}
interface InvokeMap1x4 {
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (): InvokeMap1x4;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (methodName: string): InvokeMap1x5;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (p1: _.__, args: ReadonlyArray<any>): InvokeMap1x6;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (methodName: string, args: ReadonlyArray<any>): any[];
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    <TResult>(method: (...args: any[]) => TResult): InvokeMap2x5<TResult>;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    <TResult>(method: (...args: any[]) => TResult, args: ReadonlyArray<any>): TResult[];
}
interface InvokeMap1x5 {
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (): InvokeMap1x5;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (args: ReadonlyArray<any>): any[];
}
interface InvokeMap1x6 {
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (): InvokeMap1x6;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (methodName: string): any[];
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    <TResult>(method: (...args: any[]) => TResult): TResult[];
}
interface InvokeMap2x1<TResult> {
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (): InvokeMap2x1<TResult>;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (args: ReadonlyArray<any>): InvokeMap2x3<TResult>;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (p1: _.__, collection: object | null | undefined): InvokeMap2x5<TResult>;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (args: ReadonlyArray<any>, collection: object | null | undefined): TResult[];
}
interface InvokeMap2x3<TResult> {
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (): InvokeMap2x3<TResult>;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (collection: object | null | undefined): TResult[];
}
interface InvokeMap2x5<TResult> {
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (): InvokeMap2x5<TResult>;
    /**
    * Invokes the method named by methodName on each element in the collection returning
    * an array of the results of each invoked method. Additional arguments will be provided
    * to each invoked method. If methodName is a function it will be invoked for, and this
    * bound to, each element in the collection.
    * @param collection The collection to iterate over.
    * @param methodName The name of the method to invoke.
    * @param args Arguments to invoke the method with.
    **/
    (args: ReadonlyArray<any>): TResult[];
}

declare const invokeArgsMap: InvokeMap;
export = invokeArgsMap;

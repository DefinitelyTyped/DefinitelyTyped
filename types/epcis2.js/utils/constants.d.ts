export namespace eventEpcRelType {
    const epcList: string;
    const childEPCs: string;
    const inputEPCList: string;
    const outputEPCList: string;
    const quantityList: string;
    const inputQuantityList: string;
    const outputQuantityList: string;
    const childQuantityList: string;
    const parentID: string;
}
export namespace eventSourceDestRelType {
    const source: string;
    const destination: string;
}

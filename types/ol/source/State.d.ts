/**
 * State of the source, one of 'undefined', 'loading', 'ready' or 'error'.
 */
declare enum State {
    UNDEFINED = 'undefined',
    LOADING = 'loading',
    READY = 'ready',
    ERROR = 'error',
}

export default State;

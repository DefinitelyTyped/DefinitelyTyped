import Telnet from 'react-native-telnet-client';

// create a new Telnet instance
const connection = new Telnet();

// Testing other functionalities is rather difficult because
// a working telnet connection needs to be established.

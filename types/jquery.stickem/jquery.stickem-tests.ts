// without options
$('div').stickem();

jQuery('div').stickem();

// with options
$('div').stickem({item:
    '.stickem',
    container: '.stickem-container',
    stickClass: 'stickit',
    endStickClass: 'stickit-end'
});

jQuery('div').stickem({
    item: '.stickem',
    container: '.stickem-container',
    stickClass: 'stickit',
    endStickClass: 'stickit-end'
});
import * as History from 'html5-history';

History.init();
History.getState();
History.getCurrentIndex();
History.getStateByIndex(1);
History.getHash(0);
History.unescapeHash(0);
History.normalizeHash(0);
History.setHash(1, 2);

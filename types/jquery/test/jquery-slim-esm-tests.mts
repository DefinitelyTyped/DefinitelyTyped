import jQ from "jquery/slim";
import { $, jQuery } from "jquery/slim";

const $window = jQ(window);
const forced: JQuery<Window> = $window;

const $window2 = jQuery(window);
const $window3 = $(window);

$(document).ready(() => {
    const config: DataTables.Settings = {
        // FixedColumns extension options
        fixedColumns: {
            heightMatch: 'semiauto',
            leftColumns: 2,
            rightColumns: 1
        }
    };
});

language: node_js
node_js:
  - 8

sudo: false

notifications:
  email: false

script:
 - npm install
 - npm run build
 - npm run test
 - if [[ $TRAVIS_EVENT_TYPE == "cron" ]]; then npm run update-codeowners || travis_terminate
   1; fi
import { array as stat, matrix as matStat } from "ml-stat";

var arr: number[];
var s: number[][];
var bool: boolean;
var num: number;
var mat: number[][];

// array
stat.arithmeticMean(arr);
stat.center(arr);
stat.center(arr, bool);
stat.contraHarmonicMean(arr);
stat.covariance(arr, arr);
stat.covariance(arr, arr, bool);
stat.cumulativeSum(arr);
stat.entropy(arr);
stat.entropy(arr, num);
stat.geometricMean(arr);
stat.grandMean(arr, arr);
stat.harmonicMean(arr);
stat.kurtosis(arr, bool);
stat.logMean(arr);
stat.max(arr);
stat.mean(arr);
stat.median(arr);
stat.median(arr, bool);
stat.min(arr);
stat.minMax(arr);
stat.mode(arr);
stat.pooledStandardDeviation(s);
stat.pooledStandardDeviation(s, bool);
stat.pooledVariance(s);
stat.pooledVariance(s, bool);
stat.quartiles(arr);
stat.quartiles(arr, bool);
stat.robustMeanAndStdev(arr);
stat.skewness(arr);
stat.skewness(arr, bool);
stat.standardDeviation(arr);
stat.standardDeviation(arr, bool);
stat.standardError(arr);
stat.standardize(arr);
stat.standardize(arr, num);
stat.standardize(arr, num, bool);
stat.sum(arr);
stat.truncatedMean(arr, num);
stat.truncatedMean(arr, num, bool);
stat.variance(arr);
stat.variance(arr, bool);
stat.weightedMean(arr, arr);
stat.weightedStandardDeviation(arr, arr);
stat.weightedVariance(arr, arr);

// matrix
matStat.center(mat, arr, bool);
matStat.correlation(mat);
matStat.covariance(mat);
matStat.covariance(mat, num);
matStat.entropy(mat);
matStat.entropy(mat, num);
matStat.kurtosis(mat);
matStat.kurtosis(mat, bool);
matStat.max(mat);
matStat.mean(mat);
matStat.mean(mat, num);
matStat.median(mat);
matStat.min(mat);
matStat.minMax(mat);
matStat.mode(mat);
matStat.product(mat);
matStat.product(mat, num);
matStat.scatter(mat);
matStat.scatter(mat, num);
matStat.skewness(mat);
matStat.skewness(mat, bool);
matStat.standardDeviation(mat);
matStat.standardDeviation(mat, bool);
matStat.standardDeviation(mat, bool, arr);
matStat.standardError(mat);
matStat.standardize(mat);
matStat.standardize(mat, arr);
matStat.standardize(mat, arr, bool);
matStat.sum(mat);
matStat.sum(mat, num);
matStat.variance(mat);
matStat.variance(mat, arr);
matStat.variance(mat, arr, bool);
matStat.weightedCovariance(mat, arr);
matStat.weightedCovariance(mat, arr, num);
matStat.weightedCovariance(mat, arr, num, arr);
matStat.weightedMean(mat, arr);
matStat.weightedMean(mat, arr, num);
matStat.weightedScatter(mat, arr, arr);
matStat.weightedScatter(mat, arr, arr, num);
matStat.weightedScatter(mat, arr, arr, num, num);
matStat.weightedVariance(mat, arr);
matStat.zScores(mat);
matStat.zScores(mat, arr);
matStat.zScores(mat, arr, arr);
